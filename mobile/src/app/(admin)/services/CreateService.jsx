import { View, Text } from "react-native";
import React from "react";
import AdminCreateService from "../../../features/admin/pages/AdminCreateService";

const CreateService = () => {
  return <AdminCreateService />;
};

export default CreateService;
